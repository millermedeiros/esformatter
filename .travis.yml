 language: node_js
 node_js:
   - "0.10"
 script:
   - "npm test --coverage"
   - "npm run-script lint"
 notifications:
  irc:
    channels:
      - "irc.freenode.org#esformatter"
    on_success: always
    on_failure: always
    use_notice: true
    skip_join: true
