switch ( event.keyCode ) {
    case $.ui.keyCode.ENTER :
        case $.ui.keyCode.SPACE:        case $.ui.keyCode.DOWN_ARROW:
        // line comment
        z();
        break
    case $.ui.keyCode.ESCAPE:
                y();
            break;
    default:
    x();
}

call(function(){
    switch (fruit) {
        case Fruit.APPLE:
            // line comment
            exotic();
            break;
                default:
                unknown();
    }
});
