switch ( event.keyCode ) {
    case $.ui.keyCode.ENTER:
        case $.ui.keyCode.SPACE:
        // line comment
        z();
        break;
    case $.ui.keyCode.ESCAPE:
                y();
            break;
    default:
    x();
}

call(function(){
    switch (fruit) {
        case Fruit.APPLE:
            // line comment
            exotic();
            break;
                default:
                unknown();
    }
});
