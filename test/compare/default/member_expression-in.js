obj[ prop ] = foo . bar;

BarNamespace [ 'Bar' ] . Bar [ "Bar" ] ( 'bar' ) . Bar ( "bar", "bar" )

// should remove these line breaks
foo.
  bar.
  ipum();

// should NOT remove line breaks in this case
lorem
  .ipsum
  .dolor();
