a ? foo() : bar();

b = (dolor !== amet) ? 'ipsum' : 'dolor';

if (true) {
    c = !a ? (!foo ? d : function() {
        return a;
    }) : b;
}

// should break lines
foo.a = true;
a ? foo() : bar()

